import NoteHeader from '@features/Notes/NoteHeader';

export default {
  title: 'Notes/NoteHeader',
  component: NoteHeader,
  tags: ['autodocs'],
};

export const NoteHeaderDefault = {
  args: {
    handleClick: () => {},
  },
};

export const NoteHeaderEmpty = {
  args: {
    handleClick: () => {},
  },
};
